const _ = require("lodash");

const meeteek = input => {

  var res = [];

  _.map(input, people =>{

  })

  console.log(res)
  return JSON.stringify(res);
};

module.exports = {
  title: "Exercise 3",
  run: meeteek
};
